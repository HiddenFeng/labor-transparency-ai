{
  "name": "labor-transparency-sites",
  "version": "0.8.4-rc.3",
  "private": true,
  "type": "module",
  "description": "Sites-ready interactive public-interest labor transparency app",
  "scripts": {
    "dev": "node src/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "advisory:once": "node scripts/advisory-agent.mjs --once",
    "verify": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
